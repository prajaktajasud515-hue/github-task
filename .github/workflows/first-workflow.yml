name: First GitHub Actions Workflow

on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print welcome message
        run: echo "Hello! My first GitHub Actions workflow is running successfully."
